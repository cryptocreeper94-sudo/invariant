// ═══════════════════════════════════════════════════════════
// Verdara Ultra — Compiled Bundle
// 4/42 Deterministic Outdoor Engine
// Zero Dependencies — Built with Lume
// Source: src/lume/*.lume
// Generated by Lume Compiler v0.8.1 — Verdara Edition
// ═══════════════════════════════════════════════════════════

(function() {
"use strict";

// ═══ Lume Standard Library (Browser) ═══
const text = {
  upper: (s) => String(s).toUpperCase(),
  lower: (s) => String(s).toLowerCase(),
  trim: (s) => String(s).trim(),
  split: (s, sep) => String(s).split(sep),
  join: (arr, sep = ', ') => arr.join(sep),
  replace: (s, from, to) => String(s).replaceAll(from, to),
  contains: (s, sub) => String(s).includes(sub),
  length: (s) => String(s).length,
  pad_start: (s, len, ch) => String(s).padStart(len, ch || '0'),
  slice: (s, a, b) => String(s).slice(a, b),
};

const math = {
  abs: Math.abs, ceil: Math.ceil, floor: Math.floor, round: Math.round,
  min: (...a) => Math.min(...a), max: (...a) => Math.max(...a),
  random: () => Math.random(),
  random_int: (min, max) => Math.floor(Math.random() * (max - min + 1)) + min,
  clamp: (v, lo, hi) => Math.min(Math.max(v, lo), hi),
  sin: Math.sin, cos: Math.cos, PI: Math.PI, sqrt: Math.sqrt,
  hypot: Math.hypot, acos: Math.acos,
};

const list = {
  first: (a) => a[0], last: (a) => a[a.length - 1],
  map: (a, fn) => a.map(fn), filter: (a, fn) => a.filter(fn),
  range: (start, end) => { const r = []; for (let i = start; i < end; i++) r.push(i); return r; },
  count: (a) => a.length,
  find: (a, fn) => a.find(fn),
  sort: (a, fn) => [...a].sort(fn),
  reduce: (a, fn, init) => a.reduce(fn, init),
  includes: (a, v) => a.includes(v),
};

const dom = {
  create: (tag, opts = {}) => {
    const el = document.createElement(tag);
    if (opts.text) el.textContent = opts.text;
    if (opts.html) el.innerHTML = opts.html;
    if (opts.id) el.id = opts.id;
    if (opts.className) el.className = opts.className;
    if (opts.styles) Object.assign(el.style, opts.styles);
    if (opts.attrs) { for (const [k, v] of Object.entries(opts.attrs)) el.setAttribute(k, v); }
    if (opts.children) { for (const c of opts.children) el.appendChild(typeof c === 'string' ? document.createTextNode(c) : c); }
    if (opts.onClick) el.addEventListener('click', opts.onClick);
    return el;
  },
  select: (s) => document.querySelector(s),
  select_all: (s) => [...document.querySelectorAll(s)],
  set_text: (el, t) => { if (typeof el === 'string') el = document.querySelector(el); if (el) el.textContent = t; },
  set_html: (el, h) => { if (typeof el === 'string') el = document.querySelector(el); if (el) el.innerHTML = h; },
  set_style: (el, p, v) => { if (typeof el === 'string') el = document.querySelector(el); if (el) el.style[p] = v; },
  set_styles: (el, s) => { if (typeof el === 'string') el = document.querySelector(el); if (el) Object.assign(el.style, s); },
  add_class: (el, ...c) => { if (typeof el === 'string') el = document.querySelector(el); if (el) el.classList.add(...c); },
  remove_class: (el, ...c) => { if (typeof el === 'string') el = document.querySelector(el); if (el) el.classList.remove(...c); },
  toggle_class: (el, c) => { if (typeof el === 'string') el = document.querySelector(el); if (el) el.classList.toggle(c); },
  has_class: (el, c) => { if (typeof el === 'string') el = document.querySelector(el); return el ? el.classList.contains(c) : false; },
  on: (el, ev, fn) => { if (typeof el === 'string') el = document.querySelector(el); if (el) el.addEventListener(ev, fn); },
  mount: (el, t) => { const p = t ? (typeof t === 'string' ? document.querySelector(t) : t) : document.body; p.appendChild(el); return el; },
  inject_css: (css, id) => {
    if (id) { const e = document.getElementById(id); if (e) { e.textContent = css; return e; } }
    const s = document.createElement('style'); if (id) s.id = id; s.textContent = css; document.head.appendChild(s); return s;
  },
  remove: (el) => { if (typeof el === 'string') el = document.querySelector(el); if (el && el.parentNode) el.parentNode.removeChild(el); },
  clear: (el) => { if (typeof el === 'string') el = document.querySelector(el); if (el) while (el.firstChild) el.removeChild(el.firstChild); },
  ready: (fn) => { if (document.readyState !== 'loading') fn(); else document.addEventListener('DOMContentLoaded', fn); },
};

const state = {
  reactive: (init) => {
    let v = init; const ls = [];
    return {
      get: () => v,
      set: (nv) => { const o = v; v = nv; ls.forEach(fn => fn(v, o)); },
      update: (fn) => { const o = v; v = fn(v); ls.forEach(fn => fn(v, o)); },
      on_change: (fn) => { ls.push(fn); },
    };
  },
};

const time = {
  now: () => Date.now(),
  delay: (ms) => new Promise(r => setTimeout(r, ms)),
  interval: (fn, ms) => setInterval(fn, ms),
  clear_interval: (id) => clearInterval(id),
  ticker: (fn, ms) => { let id = setInterval(fn, ms); return { stop: () => clearInterval(id) }; },
};

const canvas = {
  create: (w, h) => { const c = document.createElement('canvas'); c.width = w; c.height = h; return { el: c, ctx: c.getContext('2d') }; },
};

// ═══ Application Code ═══

// ═══════════════════════════════════════════════════════════
// VERDARA ULTRA — 4/42 Deterministic Environmental Engine
// Lume-Native · DarkWave Studios LLC · 2026
// ═══════════════════════════════════════════════════════════

const VERDARA_META = {
  name: 'Verdara Ultra',
  version: '1.0.0',
  class: 'Lume-native deterministic engine',
  domain: 'Outdoor ultra — terrain, weather, environment, biological',
  nodes: 42,
  primitives: 4,
  product: 'verdaraultra.com',
  org: 'DarkWave Studios LLC',
  papers: 71,
  books: 3
}


// ═══════════════════════════════════════════════════════════
// VERDARA ULTRA — Design System (CSS Injection)
// ═══════════════════════════════════════════════════════════

dom.inject_css(`
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;600;700&display=swap');

:root {
  --terrain: #10b981; --terrain-dim: #059669; --terrain-glow: rgba(16,185,129,0.25);
  --weather: #06b6d4; --weather-dim: #0891b2; --weather-glow: rgba(6,182,212,0.25);
  --enviro: #f59e0b; --enviro-dim: #d97706; --enviro-glow: rgba(245,158,11,0.25);
  --bio: #f43f5e; --bio-dim: #e11d48; --bio-glow: rgba(244,63,94,0.25);
  --nominal: #10b981; --advisory: #f59e0b; --caution: #f97316; --critical: #ef4444;
  --font-body: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  --font-mono: 'JetBrains Mono', 'Fira Code', monospace;
  --gap: 16px; --radius: 14px; --radius-sm: 8px;
  --transition: 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

[data-theme='dark'] {
  --bg-primary: #080f0c; --bg-secondary: #0d1a16; --bg-tertiary: #111f1b;
  --bg-card: rgba(16,32,26,0.55); --bg-card-hover: rgba(20,40,32,0.7);
  --bg-input: rgba(16,32,26,0.4);
  --border: rgba(16,185,129,0.08); --border-hover: rgba(16,185,129,0.18);
  --text-primary: rgba(255,255,255,0.92); --text-secondary: rgba(255,255,255,0.55);
  --text-muted: rgba(255,255,255,0.25); --text-inverse: #0a1210;
  --glass-bg: rgba(10,18,16,0.75); --glass-border: rgba(16,185,129,0.1);
  --shadow: 0 8px 32px rgba(0,0,0,0.5); --shadow-lg: 0 16px 64px rgba(0,0,0,0.7);
  --glow-primary: 0 0 30px rgba(16,185,129,0.15);
  --hero-gradient: linear-gradient(160deg, #080f0c 0%, #0a1812 30%, #0d1a16 60%, #111f1b 100%);
  --ring-bg: rgba(16,185,129,0.03);
}

[data-theme='light'] {
  --bg-primary: #f7f4ee; --bg-secondary: #efe9df; --bg-tertiary: #e8e0d4;
  --bg-card: rgba(255,255,255,0.75); --bg-card-hover: rgba(255,255,255,0.9);
  --bg-input: rgba(255,255,255,0.6);
  --border: rgba(26,46,40,0.08); --border-hover: rgba(26,46,40,0.18);
  --text-primary: #1a2e28; --text-secondary: #4a6358;
  --text-muted: #8a9e94; --text-inverse: #f7f4ee;
  --glass-bg: rgba(255,255,255,0.65); --glass-border: rgba(26,46,40,0.1);
  --shadow: 0 4px 20px rgba(0,0,0,0.06); --shadow-lg: 0 12px 48px rgba(0,0,0,0.1);
  --glow-primary: 0 0 20px rgba(16,185,129,0.1);
  --hero-gradient: linear-gradient(160deg, #f7f4ee 0%, #efe9df 30%, #e8e0d4 60%, #dfd6c8 100%);
  --ring-bg: rgba(16,185,129,0.05);
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; -webkit-font-smoothing: antialiased; }
body { font-family: var(--font-body); background: var(--bg-primary); color: var(--text-primary); line-height: 1.6; min-height: 100vh; transition: background var(--transition), color var(--transition); }

.topbar { position: sticky; top: 0; z-index: 100; display: flex; align-items: center; justify-content: space-between; padding: 12px 28px; background: var(--glass-bg); backdrop-filter: blur(30px); -webkit-backdrop-filter: blur(30px); border-bottom: 1px solid var(--glass-border); }
.topbar-brand { display: flex; align-items: center; gap: 12px; }
.topbar-logo { font-size: 22px; font-weight: 900; letter-spacing: -0.03em; background: linear-gradient(135deg, var(--terrain), var(--weather)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
.topbar-tag { font-size: 10px; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; color: var(--text-muted); padding: 3px 10px; border: 1px solid var(--border); border-radius: 999px; }
.topbar-right { display: flex; align-items: center; gap: 16px; }
.equilibrium-badge { display: flex; align-items: center; gap: 8px; padding: 6px 14px; background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius-sm); font-family: var(--font-mono); font-size: 13px; font-weight: 600; }
.eq-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--nominal); animation: pulse-dot 2s ease-in-out infinite; }
@keyframes pulse-dot { 0%, 100% { opacity: 1; box-shadow: 0 0 0 0 rgba(16,185,129,0.4); } 50% { opacity: 0.8; box-shadow: 0 0 0 6px rgba(16,185,129,0); } }
.theme-toggle { background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius-sm); color: var(--text-secondary); cursor: pointer; font-size: 18px; width: 38px; height: 38px; display: flex; align-items: center; justify-content: center; transition: all var(--transition); }
.theme-toggle:hover { border-color: var(--border-hover); background: var(--bg-card-hover); transform: scale(1.05); }

.hero { position: relative; padding: 48px 28px 32px; text-align: center; background: var(--hero-gradient); overflow: hidden; }
.hero::before { content: ''; position: absolute; top: -40%; left: 50%; transform: translateX(-50%); width: 800px; height: 400px; background: radial-gradient(ellipse, var(--terrain-glow) 0%, transparent 70%); pointer-events: none; }
.hero-title { font-size: clamp(28px, 5vw, 48px); font-weight: 900; letter-spacing: -0.04em; line-height: 1.1; margin-bottom: 10px; background: linear-gradient(135deg, var(--terrain) 0%, var(--weather) 50%, var(--enviro) 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
.hero-sub { font-size: 14px; color: var(--text-secondary); font-weight: 400; max-width: 540px; margin: 0 auto; }

.main { max-width: 1320px; margin: 0 auto; padding: 32px 24px; display: flex; flex-direction: column; gap: 32px; }

.explainer { background: var(--bg-card); backdrop-filter: blur(20px); border: 1px solid var(--border); border-radius: var(--radius); padding: 24px; }
.explainer-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; }
.explainer-step { padding: 20px; border-radius: var(--radius-sm); background: var(--bg-input); border: 1px solid var(--border); transition: all var(--transition); }
.explainer-step:hover { border-color: var(--border-hover); transform: translateY(-2px); }
.step-num { font-family: var(--font-mono); font-size: 28px; font-weight: 900; background: linear-gradient(135deg, var(--terrain), var(--weather)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; margin-bottom: 8px; }
.step-title { font-size: 14px; font-weight: 800; margin-bottom: 6px; }
.step-desc { font-size: 12px; color: var(--text-secondary); line-height: 1.5; }
.step-desc strong { color: var(--text-primary); }

.organism-section { display: flex; gap: 24px; align-items: flex-start; }
.ring-wrapper { flex: 1; min-width: 0; }
.ring-mode-toggle { display: flex; gap: 4px; margin-bottom: 12px; justify-content: center; }
.mode-btn { background: var(--bg-input); border: 1px solid var(--border); border-radius: var(--radius-sm); padding: 6px 16px; font-family: var(--font-mono); font-size: 10px; font-weight: 700; letter-spacing: 0.08em; color: var(--text-muted); cursor: pointer; transition: all var(--transition); }
.mode-btn:hover { border-color: var(--border-hover); color: var(--text-secondary); }
.mode-btn.active { background: var(--terrain); color: white; border-color: var(--terrain); }

.ring-container { position: relative; aspect-ratio: 1; max-width: 620px; margin: 0 auto; background: var(--ring-bg); border-radius: 50%; box-shadow: var(--glow-primary); transition: background var(--transition); }
.ring-canvas { width: 100%; height: 100%; cursor: crosshair; }
.ring-center-label { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); text-align: center; pointer-events: none; }
.ring-center-label .score { font-family: var(--font-mono); font-size: 32px; font-weight: 900; color: var(--text-primary); }
.ring-center-label .label { font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.15em; color: var(--text-muted); margin-top: 2px; }

.ring-3d-container { max-width: 620px; aspect-ratio: 1; margin: 0 auto; perspective: 1200px; position: relative; overflow: hidden; border-radius: var(--radius); background: var(--ring-bg); cursor: grab; }
.ring-3d-container:active { cursor: grabbing; }
.sphere-scene { width: 100%; height: 100%; position: relative; transform-style: preserve-3d; }
.sphere-node { position: absolute; top: 50%; left: 50%; transform-style: preserve-3d; pointer-events: auto; cursor: pointer; }
.sphere-node-inner { backface-visibility: hidden; display: flex; align-items: center; gap: 6px; }
.sphere-node-dot { width: 14px; height: 14px; border-radius: 50%; border: 2px solid; transition: transform 0.2s, box-shadow 0.3s; flex-shrink: 0; }
.sphere-node:hover .sphere-node-dot { transform: scale(1.5); }
.sphere-node-label { font-family: var(--font-mono); font-size: 9px; font-weight: 700; white-space: nowrap; pointer-events: none; backface-visibility: hidden; }

.inspector { width: 340px; flex-shrink: 0; background: var(--bg-card); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); border: 1px solid var(--border); border-radius: var(--radius); padding: 20px; transition: all var(--transition); max-height: 620px; overflow-y: auto; }
.inspector-header { display: flex; align-items: center; gap: 10px; margin-bottom: 16px; padding-bottom: 12px; border-bottom: 1px solid var(--border); }
.inspector-icon { width: 36px; height: 36px; border-radius: var(--radius-sm); display: flex; align-items: center; justify-content: center; font-family: var(--font-mono); font-size: 12px; font-weight: 800; }
.inspector-name { font-size: 14px; font-weight: 800; }
.inspector-flow { font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; color: var(--text-muted); }
.inspector-gauge { margin: 16px 0; height: 8px; background: var(--bg-input); border-radius: 999px; position: relative; overflow: hidden; }
.inspector-gauge-fill { height: 100%; border-radius: 999px; transition: width 0.5s ease, background 0.3s ease; }
.inspector-thresholds { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 8px; margin-top: 16px; }
.threshold-card { padding: 10px; border-radius: var(--radius-sm); text-align: center; background: var(--bg-input); border: 1px solid var(--border); }
.threshold-label { font-size: 9px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: var(--text-muted); margin-bottom: 4px; }
.threshold-value { font-family: var(--font-mono); font-size: 12px; font-weight: 600; }
.inspector-sparkline { height: 48px; background: var(--bg-input); border-radius: var(--radius-sm); border: 1px solid var(--border); overflow: hidden; }
.inspector-empty { display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 200px; color: var(--text-muted); font-size: 13px; text-align: center; gap: 8px; }
.inspector-empty-icon { font-size: 24px; font-weight: 900; opacity: 0.3; color: var(--terrain); }

.flow-dashboard { display: grid; grid-template-columns: repeat(4, 1fr); gap: var(--gap); }
.flow-card { background: var(--bg-card); backdrop-filter: blur(20px); border: 1px solid var(--border); border-radius: var(--radius); padding: 20px; transition: all var(--transition); position: relative; overflow: hidden; }
.flow-card::before { content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px; border-radius: var(--radius) var(--radius) 0 0; }
.flow-card[data-flow='terrain']::before { background: var(--terrain); }
.flow-card[data-flow='weather']::before { background: var(--weather); }
.flow-card[data-flow='enviro']::before { background: var(--enviro); }
.flow-card[data-flow='bio']::before { background: var(--bio); }
.flow-card-img { width: 100%; height: 80px; object-fit: cover; border-radius: var(--radius-sm); margin-bottom: 14px; opacity: 0.7; transition: opacity var(--transition); }
[data-theme='light'] .flow-card-img { opacity: 0.85; }
.flow-card:hover .flow-card-img { opacity: 1; }
.flow-card:hover { border-color: var(--border-hover); transform: translateY(-2px); box-shadow: var(--shadow); }
.flow-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 14px; }
.flow-title { font-size: 12px; font-weight: 800; text-transform: uppercase; letter-spacing: 0.1em; }
.flow-status { font-family: var(--font-mono); font-size: 10px; font-weight: 700; padding: 3px 8px; border-radius: 999px; text-transform: uppercase; letter-spacing: 0.05em; }
.status-nominal { background: rgba(16,185,129,0.12); color: var(--nominal); }
.status-advisory { background: rgba(245,158,11,0.12); color: var(--advisory); }
.status-caution { background: rgba(249,115,22,0.12); color: var(--caution); }
.status-critical { background: rgba(239,68,68,0.12); color: var(--critical); }
.flow-pressure { font-family: var(--font-mono); font-size: 28px; font-weight: 900; margin-bottom: 12px; }
.flow-nodes { display: grid; grid-template-columns: repeat(auto-fill, minmax(100%, 1fr)); gap: 6px; }
.flow-node-row { display: flex; align-items: center; justify-content: space-between; padding: 4px 0; font-size: 11px; border-bottom: 1px solid var(--border); }
.flow-node-row:last-child { border-bottom: none; }
.flow-node-name { color: var(--text-secondary); font-weight: 500; }
.flow-node-val { font-family: var(--font-mono); font-weight: 600; font-size: 11px; }

.routing-section { background: var(--bg-card); backdrop-filter: blur(20px); border: 1px solid var(--border); border-radius: var(--radius); padding: 24px; }
.section-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 20px; }
.section-title { font-size: 16px; font-weight: 800; display: flex; align-items: center; gap: 10px; }
.section-subtitle { font-size: 11px; color: var(--text-muted); font-weight: 500; }
.routing-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; }
.routing-inputs { display: flex; flex-direction: column; gap: 12px; }
.input-group { display: flex; flex-direction: column; gap: 4px; }
.input-label { font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; color: var(--text-muted); }
.input-field { background: var(--bg-input); border: 1px solid var(--border); border-radius: var(--radius-sm); padding: 10px 14px; font-family: var(--font-mono); font-size: 13px; color: var(--text-primary); outline: none; transition: all var(--transition); }
.input-field:focus { border-color: var(--terrain); box-shadow: 0 0 0 3px var(--terrain-glow); }
.input-field::placeholder { color: var(--text-muted); }
.routing-output { display: flex; flex-direction: column; gap: 12px; }
.route-score { display: grid; grid-template-columns: repeat(2, 1fr); gap: 10px; }
.score-card { padding: 14px; border-radius: var(--radius-sm); background: var(--bg-input); border: 1px solid var(--border); text-align: center; }
.score-value { font-family: var(--font-mono); font-size: 22px; font-weight: 900; }
.score-label { font-size: 9px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; color: var(--text-muted); margin-top: 4px; }
.route-warnings { padding: 12px 16px; border-radius: var(--radius-sm); background: rgba(244,63,94,0.06); border: 1px solid rgba(244,63,94,0.12); font-size: 12px; color: var(--text-secondary); }
.route-warnings strong { color: var(--caution); font-weight: 700; }
.btn { padding: 12px 24px; border-radius: var(--radius-sm); border: none; font-family: var(--font-body); font-size: 13px; font-weight: 700; cursor: pointer; transition: all var(--transition); text-transform: uppercase; letter-spacing: 0.06em; }
.btn-primary { background: linear-gradient(135deg, var(--terrain), var(--terrain-dim)); color: white; box-shadow: 0 4px 16px var(--terrain-glow); }
.btn-primary:hover { transform: translateY(-2px); box-shadow: 0 6px 24px var(--terrain-glow); }

.scenarios-section { background: var(--bg-card); backdrop-filter: blur(20px); border: 1px solid var(--border); border-radius: var(--radius); padding: 24px; }
.scenario-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 12px; }
.scenario-card { padding: 0; border-radius: var(--radius-sm); background: var(--bg-input); border: 1px solid var(--border); cursor: pointer; transition: all var(--transition); text-align: center; overflow: hidden; position: relative; }
.scenario-card:hover { border-color: var(--border-hover); transform: translateY(-3px); box-shadow: var(--shadow); }
.scenario-card.active { border-color: var(--terrain); box-shadow: var(--glow-primary); }
.scenario-img { width: 100%; height: 90px; object-fit: cover; display: block; opacity: 0.75; transition: opacity var(--transition); }
[data-theme='light'] .scenario-img { opacity: 0.9; }
.scenario-card:hover .scenario-img { opacity: 1; }
.scenario-info { padding: 10px 12px 14px; }
.scenario-name { font-size: 12px; font-weight: 700; margin-bottom: 4px; }
.scenario-desc { font-size: 10px; color: var(--text-muted); line-height: 1.4; }
.scenario-run-bar { display: flex; align-items: center; justify-content: space-between; margin-top: 16px; padding-top: 16px; border-top: 1px solid var(--border); }
.scenario-status { font-family: var(--font-mono); font-size: 12px; color: var(--text-muted); }

.footer { text-align: center; padding: 32px 24px; border-top: 1px solid var(--border); margin-top: 16px; }
.footer-text { font-size: 10px; color: var(--text-muted); font-weight: 500; letter-spacing: 0.04em; }
.footer-text a { color: var(--terrain); text-decoration: none; }

@media (max-width: 1100px) { .organism-section { flex-direction: column; } .inspector { width: 100%; max-height: none; } .flow-dashboard { grid-template-columns: repeat(2, 1fr); } .scenario-grid { grid-template-columns: repeat(3, 1fr); } .explainer-grid { grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 640px) { .topbar { padding: 10px 16px; } .topbar-tag { display: none; } .main { padding: 20px 14px; } .flow-dashboard { grid-template-columns: 1fr; } .routing-grid { grid-template-columns: 1fr; } .scenario-grid { grid-template-columns: repeat(2, 1fr); } .explainer-grid { grid-template-columns: 1fr; } .hero { padding: 32px 16px 24px; } }
::-webkit-scrollbar { width: 6px; } ::-webkit-scrollbar-track { background: transparent; } ::-webkit-scrollbar-thumb { background: var(--border); border-radius: 999px; } ::-webkit-scrollbar-thumb:hover { background: var(--border-hover); }
@keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
.fade-in { animation: fadeIn 0.5s ease forwards; }
`, "verdara-styles")


// ═══════════════════════════════════════════════════════════
// VERDARA ULTRA — Node Data & Flow Definitions
// ═══════════════════════════════════════════════════════════

const FLOWS = {
  terrain: { color: '#10b981', glow: 'rgba(16,185,129,0.25)', label: 'Terrain Flow' },
  weather: { color: '#06b6d4', glow: 'rgba(6,182,212,0.25)', label: 'Weather Flow' },
  enviro:  { color: '#f59e0b', glow: 'rgba(245,158,11,0.25)', label: 'Environmental Flow' },
  bio:     { color: '#f43f5e', glow: 'rgba(244,63,94,0.25)', label: 'Biological Flow' }
}

const NODES = [
  { id:'T1',  flow:'terrain', name:'Elevation',         advisory:'10k ft', caution:'12k ft', critical:'14k ft' },
  { id:'T2',  flow:'terrain', name:'Slope/Grade',       advisory:'15%',    caution:'25%',    critical:'35%' },
  { id:'T3',  flow:'terrain', name:'Terrain Type',      advisory:'gravel', caution:'scree',  critical:'ice' },
  { id:'T4',  flow:'terrain', name:'Surface Stability', advisory:'moderate',caution:'low',   critical:'collapse' },
  { id:'T5',  flow:'terrain', name:'Technicality',      advisory:'Class 2',caution:'Class 3',critical:'Class 4-5' },
  { id:'T6',  flow:'terrain', name:'Trail Quality',     advisory:'faint', caution:'intermittent',critical:'off-trail' },
  { id:'T7',  flow:'terrain', name:'Obstacle Density',  advisory:'moderate',caution:'high',  critical:'impassable' },
  { id:'T8',  flow:'terrain', name:'Traction Quality',  advisory:'reduced',caution:'poor',   critical:'none' },
  { id:'T9',  flow:'terrain', name:'Water Crossing',    advisory:'ankle', caution:'knee',    critical:'waist+' },
  { id:'T10', flow:'terrain', name:'Route Ambiguity',   advisory:'2-3',   caution:'4-5',     critical:'>5' },
  { id:'W1',  flow:'weather', name:'Temperature',       advisory:'<40/>80F',caution:'<32/>90F',critical:'<20/>100F' },
  { id:'W2',  flow:'weather', name:'Wind Speed',        advisory:'15 mph', caution:'25 mph', critical:'40 mph' },
  { id:'W3',  flow:'weather', name:'Wind Direction',    advisory:'headwind',caution:'crosswind',critical:'gusting' },
  { id:'W4',  flow:'weather', name:'Precipitation Type',advisory:'light', caution:'heavy',   critical:'hail/whiteout' },
  { id:'W5',  flow:'weather', name:'Precip Intensity',  advisory:'moderate',caution:'heavy',  critical:'extreme' },
  { id:'W6',  flow:'weather', name:'Storm Proximity',   advisory:'10 mi', caution:'5 mi',    critical:'2 mi' },
  { id:'W7',  flow:'weather', name:'Lightning Risk',    advisory:'elevated',caution:'high',   critical:'imminent' },
  { id:'W8',  flow:'weather', name:'Thermal Load',      advisory:'moderate',caution:'high',   critical:'extreme' },
  { id:'W9',  flow:'weather', name:'Weather Volatility',advisory:'unstable',caution:'rapid',  critical:'chaotic' },
  { id:'W10', flow:'weather', name:'Safe Window',       advisory:'<2h',   caution:'<1h',     critical:'<30m' },
  { id:'E1',  flow:'enviro', name:'Humidity',           advisory:'70%',   caution:'85%',     critical:'95%' },
  { id:'E2',  flow:'enviro', name:'Dew Point',          advisory:'10F delta',caution:'5F delta', critical:'= temp' },
  { id:'E3',  flow:'enviro', name:'Fog/Cloud',          advisory:'patchy',caution:'dense',   critical:'whiteout' },
  { id:'E4',  flow:'enviro', name:'Visibility',         advisory:'500m',  caution:'200m',    critical:'50m' },
  { id:'E5',  flow:'enviro', name:'Sunlight Intensity', advisory:'low',   caution:'very low',critical:'dark' },
  { id:'E6',  flow:'enviro', name:'UV Index',           advisory:'6',     caution:'8',       critical:'10' },
  { id:'E7',  flow:'enviro', name:'Air Quality',        advisory:'AQI 75',caution:'AQI 125', critical:'AQI 175' },
  { id:'E8',  flow:'enviro', name:'Pressure Trend',     advisory:'falling',caution:'rapid',   critical:'collapse' },
  { id:'E9',  flow:'enviro', name:'Day/Night Phase',    advisory:'dusk',  caution:'twilight', critical:'night' },
  { id:'E10', flow:'enviro', name:'Light Pollution',    advisory:'moderate',caution:'low',    critical:'none' },
  { id:'E11', flow:'enviro', name:'Sensory Clarity',    advisory:'reduced',caution:'poor',    critical:'extreme poor' },
  { id:'B1',  flow:'bio', name:'Wildlife Density',      advisory:'moderate',caution:'high',   critical:'very high' },
  { id:'B2',  flow:'bio', name:'Wildlife Risk',         advisory:'possible',caution:'likely',  critical:'confirmed' },
  { id:'B3',  flow:'bio', name:'Insect Density',        advisory:'moderate',caution:'heavy',   critical:'extreme' },
  { id:'B4',  flow:'bio', name:'Pathogen Risk',         advisory:'suspected',caution:'likely',  critical:'confirmed' },
  { id:'B5',  flow:'bio', name:'Vegetation Density',    advisory:'moderate',caution:'heavy',   critical:'impassable' },
  { id:'B6',  flow:'bio', name:'Edible Resources',      advisory:'scarce', caution:'very scarce',critical:'none' },
  { id:'B7',  flow:'bio', name:'Water Availability',    advisory:'3 mi',  caution:'6 mi',    critical:'10 mi' },
  { id:'B8',  flow:'bio', name:'Allergen Load',         advisory:'moderate',caution:'high',   critical:'extreme' },
  { id:'B9',  flow:'bio', name:'Human Density',         advisory:'moderate',caution:'high',   critical:'very high' },
  { id:'B10', flow:'bio', name:'Land Use Constraints',  advisory:'advisory',caution:'restricted',critical:'illegal' },
  { id:'B11', flow:'bio', name:'Rescue Accessibility',  advisory:'2h',    caution:'4h',      critical:'8h' },
]

const SCENARIOS = [
  { id:'alpine', img:'img/sc-alpine.jpg', name:'Alpine Storm', desc:'W6/W7 escalating, steep terrain, scarce water',
    overrides: { W6:-0.6, W7:-0.8, W9:-0.3, T2:-0.5, T5:-0.4, B7:-0.7, E8:-0.6, W2:-0.5 }},
  { id:'desert', img:'img/sc-desert.jpg', name:'Desert Crossing', desc:'Extreme heat, high UV, critical water',
    overrides: { W1:-0.7, W8:-0.9, E6:-0.8, B7:-1.0, E1:-0.2, B6:-0.8, E4:0.9, T6:-0.3 }},
  { id:'forest', img:'img/sc-forest.jpg', name:'Night Forest', desc:'Low visibility, wildlife active, off-trail',
    overrides: { E9:-0.7, E4:-0.6, E5:-0.8, B1:-0.5, B2:-0.4, T6:-0.6, E10:0.8, B3:-0.4 }},
  { id:'flood', img:'img/sc-flood.jpg', name:'River Flood', desc:'Extreme water crossings, heavy rain',
    overrides: { T9:-0.9, W4:-0.7, W5:-0.8, T4:-0.6, T8:-0.7, E3:-0.5, B7:0.9, E8:-0.5 }},
  { id:'ridge', img:'img/sc-ridge.jpg', name:'High Ridge', desc:'14k+ altitude, Class 4, 40mph winds',
    overrides: { T1:-0.8, T5:-0.7, W2:-0.9, T2:-0.6, W8:-0.4, E1:-0.3, B11:-0.6, W9:-0.5 }},
]


// ═══════════════════════════════════════════════════════════
// VERDARA ULTRA — State Engine
// ═══════════════════════════════════════════════════════════

const organism = { nodes: {}, history: {}, selected: null, scenario: null, tick: 0, is3D: false }

function initOrganism() {
  for (const n of NODES) {
    organism.nodes[n.id] = 0.5 + math.random() * 0.5
    organism.history[n.id] = []
  }
}

function getStatus(val) {
  if (val >= 0.3) return 'nominal'
  if (val >= -0.1) return 'advisory'
  if (val >= -0.5) return 'caution'
  return 'critical'
}

function getStatusColor(status) {
  return { nominal: '#10b981', advisory: '#f59e0b', caution: '#f97316', critical: '#ef4444' }[status]
}

function flowPressure(flowId) {
  const flowNodes = NODES.filter(n => n.flow === flowId)
  const sum = flowNodes.reduce((s, n) => s + organism.nodes[n.id], 0)
  return sum / flowNodes.length
}

function flowStatus(flowId) {
  return getStatus(flowPressure(flowId))
}

function equilibrium() {
  const all = Object.values(organism.nodes)
  return all.reduce((s, v) => s + v, 0) / all.length
}

function applyScenario(scenarioId) {
  for (const n of NODES) {
    organism.nodes[n.id] = 0.5 + math.random() * 0.3
  }
  const sc = SCENARIOS.find(s => s.id === scenarioId)
  if (!sc) return
  for (const [id, val] of Object.entries(sc.overrides)) {
    if (organism.nodes[id] !== undefined) organism.nodes[id] = val
  }
}

function tickOrganism() {
  organism.tick++
  for (const n of NODES) {
    const drift = (math.random() - 0.5) * 0.04
    organism.nodes[n.id] = math.clamp(organism.nodes[n.id] + drift, -1, 1)
    organism.history[n.id].push(organism.nodes[n.id])
    if (organism.history[n.id].length > 30) organism.history[n.id].shift()
  }
}

function computeRoute() {
  const risk = math.max(0, 1 + math.min(flowPressure('terrain'), flowPressure('weather')))
  const effort = math.max(0, 1 - math.abs(flowPressure('terrain')) * 0.5)
  const progress = math.max(0, (1 + equilibrium()) / 2)
  const resilience = math.max(0, (1 + math.min(flowPressure('bio'), flowPressure('enviro'))) / 2)
  const warnings = []
  for (const n of NODES) {
    const s = getStatus(organism.nodes[n.id])
    if (s === 'critical') warnings.push('[CRITICAL] ' + n.name + ' — halt/reroute')
    else if (s === 'caution') warnings.push('[CAUTION] ' + n.name + ' — monitor closely')
  }
  return { risk: risk.toFixed(2), effort: effort.toFixed(2), progress: progress.toFixed(2), resilience: resilience.toFixed(2), warnings }
}


// ═══════════════════════════════════════════════════════════
// VERDARA ULTRA — Dashboard, Inspector, Equilibrium
// ═══════════════════════════════════════════════════════════

function updateInspector() {
  const empty = dom.select('#inspector-empty')
  const content = dom.select('#inspector-content')
  if (!organism.selected) {
    empty.style.display = 'flex'; content.style.display = 'none'; return
  }
  empty.style.display = 'none'; content.style.display = 'block'
  const node = NODES.find(n => n.id === organism.selected)
  const val = organism.nodes[node.id]
  const status = getStatus(val)
  const col = getStatusColor(status)
  const flowCol = FLOWS[node.flow].color

  dom.set_text('#insp-icon', node.id)
  dom.set_styles('#insp-icon', { background: FLOWS[node.flow].glow, color: flowCol })
  dom.set_text('#insp-name', node.id + ' — ' + node.name)
  dom.set_text('#insp-flow', FLOWS[node.flow].label)
  dom.set_style('#insp-flow', 'color', flowCol)
  dom.set_text('#insp-value', (val >= 0 ? '+' : '') + val.toFixed(3))
  dom.set_style('#insp-value', 'color', col)

  const gauge = dom.select('#insp-gauge')
  const pct = ((val + 1) / 2) * 100
  gauge.style.width = pct + '%'
  gauge.style.background = 'linear-gradient(90deg, ' + col + ', ' + flowCol + ')'

  dom.set_text('#insp-advisory', node.advisory)
  dom.set_text('#insp-caution', node.caution)
  dom.set_text('#insp-critical', node.critical)

  const statusEl = dom.select('#insp-status')
  statusEl.textContent = text.upper(status)
  statusEl.style.background = col + '18'
  statusEl.style.color = col

  drawSparkline()
}

function drawSparkline() {
  if (!organism.selected) return
  const c = dom.select('#insp-sparkline')
  const sctx = c.getContext('2d')
  const w = c.offsetWidth
  const h = 48
  c.width = w * 2; c.height = h * 2
  c.style.width = w + 'px'; c.style.height = h + 'px'
  sctx.setTransform(2, 0, 0, 2, 0, 0)
  sctx.clearRect(0, 0, w, h)

  const hist = organism.history[organism.selected] || []
  if (hist.length < 2) return
  const node = NODES.find(n => n.id === organism.selected)
  const col = FLOWS[node.flow].color

  sctx.beginPath()
  hist.forEach((v, i) => {
    const x = (i / (hist.length - 1)) * w
    const y = h - ((v + 1) / 2) * h
    i === 0 ? sctx.moveTo(x, y) : sctx.lineTo(x, y)
  })
  sctx.strokeStyle = col
  sctx.lineWidth = 1.5
  sctx.stroke()
  sctx.lineTo(w, h)
  sctx.lineTo(0, h)
  sctx.closePath()
  sctx.fillStyle = col + '15'
  sctx.fill()
}

function updateDashboard() {
  const flowIds = ['terrain','weather','enviro','bio']
  for (const f of flowIds) {
    const p = flowPressure(f)
    const s = flowStatus(f)
    dom.set_text('#' + f + '-pressure', (p >= 0 ? '+' : '') + p.toFixed(2))
    const statusEl = dom.select('#' + f + '-status')
    statusEl.textContent = text.upper(s)
    statusEl.className = 'flow-status status-' + s

    const container = dom.select('#' + f + '-nodes')
    const flowNodes = NODES.filter(n => n.flow === f)
    container.innerHTML = flowNodes.map(n => {
      const v = organism.nodes[n.id]
      const ns = getStatus(v)
      const c = getStatusColor(ns)
      return '<div class="flow-node-row"><span class="flow-node-name">' + n.id + ' ' + n.name + '</span><span class="flow-node-val" style="color:' + c + '">' + (v >= 0 ? '+' : '') + v.toFixed(2) + '</span></div>'
    }).join('')
  }
}

function updateEquilibrium() {
  const eq = equilibrium()
  const s = getStatus(eq)
  const c = getStatusColor(s)
  dom.set_text('#ring-score', (eq >= 0 ? '+' : '') + eq.toFixed(2))
  dom.set_style('#ring-score', 'color', c)
  dom.set_text('#eq-score', 'EQUILIBRIUM: ' + (eq >= 0 ? '+' : '') + eq.toFixed(2))
  dom.set_style('#eq-dot', 'background', c)
}


// ═══════════════════════════════════════════════════════════
// VERDARA ULTRA — UI Controls (Routing, Scenarios, Theme)
// ═══════════════════════════════════════════════════════════

function initRouting() {
  dom.on('#btn-route', 'click', function() {
    const r = computeRoute()
    dom.set_text('#score-risk', r.risk)
    dom.set_text('#score-effort', r.effort)
    dom.set_text('#score-progress', r.progress)
    dom.set_text('#score-resilience', r.resilience)
    const wEl = dom.select('#route-warnings')
    if (r.warnings.length === 0) {
      wEl.innerHTML = '<strong style="color:var(--nominal)">ALL CLEAR</strong> — No constraints violated. Route is deterministically safe.'
    } else {
      wEl.innerHTML = r.warnings.slice(0, 6).map(w => '<div style="margin-bottom:4px">' + w + '</div>').join('') +
        (r.warnings.length > 6 ? '<div style="color:var(--text-muted);margin-top:6px">+' + (r.warnings.length - 6) + ' more warnings</div>' : '')
    }
  })
}

function initScenarios() {
  const grid = dom.select('#scenario-grid')
  grid.innerHTML = SCENARIOS.map(s =>
    '<div class="scenario-card" data-id="' + s.id + '" id="sc-' + s.id + '">' +
      '<img class="scenario-img" src="' + s.img + '" alt="' + s.name + '">' +
      '<div class="scenario-info">' +
        '<div class="scenario-name">' + s.name + '</div>' +
        '<div class="scenario-desc">' + s.desc + '</div>' +
      '</div>' +
    '</div>'
  ).join('')

  const cards = dom.select_all('.scenario-card')
  for (const card of cards) {
    card.addEventListener('click', function() {
      organism.scenario = card.dataset.id
      for (const c of cards) { c.classList.remove('active') }
      card.classList.add('active')
      dom.select('#btn-scenario').disabled = false
      dom.set_text('#scenario-status', 'Selected: ' + SCENARIOS.find(s => s.id === organism.scenario).name)
    })
  }

  dom.on('#btn-scenario', 'click', function() {
    if (!organism.scenario) return
    const sc = SCENARIOS.find(s => s.id === organism.scenario)
    applyScenario(organism.scenario)
    dom.set_text('#scenario-status', 'Running: ' + sc.name + ' — organism adapting...')
    if (organism.is3D) buildSphere()
    setTimeout(function() {
      dom.set_text('#scenario-status', sc.name + ' applied — observe the ring')
    }, 800)
  })
}

function initThemeToggle() {
  dom.on('#theme-toggle', 'click', function() {
    const html = document.documentElement
    const current = html.getAttribute('data-theme')
    const next = current === 'dark' ? 'light' : 'dark'
    html.setAttribute('data-theme', next)
    dom.set_text('#theme-toggle', next === 'dark' ? '\u263D' : '\u2600')
  })
}

function initModeToggle() {
  dom.on('#btn-2d', 'click', function() {
    organism.is3D = false
    dom.set_style('#ring-2d', 'display', '')
    dom.set_style('#ring-3d', 'display', 'none')
    dom.add_class('#btn-2d', 'active')
    dom.remove_class('#btn-3d', 'active')
  })

  dom.on('#btn-3d', 'click', function() {
    organism.is3D = true
    dom.set_style('#ring-2d', 'display', 'none')
    dom.set_style('#ring-3d', 'display', '')
    dom.remove_class('#btn-2d', 'active')
    dom.add_class('#btn-3d', 'active')
    sphereAutoRotate = true
    buildSphere()
    initSphereDrag()
  })
}


// ═══════════════════════════════════════════════════════════
// VERDARA ULTRA — Boot Sequence
// ═══════════════════════════════════════════════════════════

dom.ready(function() {
  // Initialize organism state
  initOrganism()

  // Initialize 2D ring canvas
  initRingCanvas()
  initRingClick()

  // Initialize UI controls
  initRouting()
  initScenarios()
  initThemeToggle()
  initModeToggle()

  // Initial renders
  updateDashboard()
  updateEquilibrium()
  drawRing()

  // Main loop — 500ms tick
  time.interval(function() {
    tickOrganism()
    if (!organism.is3D) {
      drawRing()
    } else {
      tickSphere()
      updateSphereColors()
    }
    updateEquilibrium()
    if (organism.selected) updateInspector()
    if (organism.tick % 10 === 0) updateDashboard()
  }, 500)

  // Resize handler
  window.addEventListener('resize', function() {
    if (!organism.is3D) {
      resizeRingCanvas()
      drawRing()
    } else {
      buildSphere()
    }
  })

  console.log('Verdara Ultra v' + VERDARA_META.version + ' — ' + VERDARA_META.nodes + ' nodes online')
})


// ═══ Lume Health Beacon ═══
window.__LUME_HEALTH__ = {
  status: 'ok',
  rendered: Date.now(),
  version: '0.8.1',
  bundle: 'verdara',
  product: 'Verdara Ultra',
  stages: { syntax: true, structure: true, execution: true }
};

})();
